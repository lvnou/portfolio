#!/usr/bin/env python

from distutils.core import setup
import setuptools

setup(name='portfolio',
      version='0.1',
      description='Portfolio Management',
      author='Erik Natkowski',
      packages=setuptools.find_packages(),
     )
